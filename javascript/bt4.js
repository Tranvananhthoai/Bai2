let so = "42";
console.log("Kiểu dữ liệu trước khi chuyển đổi:" ,typeof so);
let number = Number(so);
let result =Number(so)+8;
console.log ("Kết quả sau khi cộng với 8:", result);
console.log ("Kiểu dữ liệu sau khi chuyển đổi:", typeof result);

